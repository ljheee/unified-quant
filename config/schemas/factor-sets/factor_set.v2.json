{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://unified-quant.local/schemas/factor-sets/factor_set.v2.json",
  "title": "Reviewed factor set definition v2",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "set_definition_version",
    "factor_set",
    "factor_version",
    "status",
    "required_columns",
    "quality_policy",
    "dependencies",
    "factors"
  ],
  "properties": {
    "set_definition_version": {
      "const": 2
    },
    "factor_set": {
      "type": "string",
      "minLength": 1
    },
    "factor_version": {
      "$ref": "#/$defs/semver"
    },
    "status": {
      "enum": [
        "draft",
        "reviewed"
      ]
    },
    "required_columns": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": {
        "type": "string"
      }
    },
    "quality_policy": {
      "enum": [
        "reject_all",
        "accept_with_warnings"
      ]
    },
    "quality_thresholds": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "null_rate_maximum",
        "coverage_minimum"
      ],
      "properties": {
        "null_rate_maximum": {
          "type": "number",
          "minimum": 0,
          "maximum": 1
        },
        "coverage_minimum": {
          "type": "number",
          "minimum": 0,
          "maximum": 1
        }
      }
    },
    "dependencies": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^[^/]+/\\d+\\.\\d+\\.\\d+$"
      }
    },
    "qlib_expression_set": {
      "type": "string",
      "enum": [
        "Alpha158",
        "Alpha360"
      ]
    },
    "excluded_factors": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "name",
          "reason"
        ],
        "properties": {
          "name": {
            "type": "string",
            "minLength": 1
          },
          "reason": {
            "type": "string",
            "minLength": 1
          }
        }
      }
    },
    "factors": {
      "type": "array",
      "minItems": 0,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "name",
          "version",
          "implementation_fingerprint",
          "qlib_name",
          "expression"
        ],
        "properties": {
          "name": {
            "type": "string",
            "minLength": 1
          },
          "version": {
            "$ref": "#/$defs/semver"
          },
          "implementation_fingerprint": {
            "$ref": "#/$defs/sha256"
          },
          "qlib_name": {
            "type": "string",
            "minLength": 1
          },
          "expression": {
            "type": "string",
            "minLength": 1
          },
          "minimum_history": {
            "type": "integer",
            "minimum": 1
          }
        }
      }
    }
  },
  "$defs": {
    "sha256": {
      "type": "string",
      "pattern": "^[0-9a-f]{64}$"
    },
    "semver": {
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d+$"
    }
  }
}
